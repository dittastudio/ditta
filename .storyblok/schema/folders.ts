import { defineFolder } from '@storyblok/schema';

export const blocksFolder = defineFolder({
  name: 'Blocks',
});

export const elementsFolder = defineFolder({
  name: 'Elements',
});

export const typesFolder = defineFolder({
  name: 'Types',
});
